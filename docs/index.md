# Documentation Index: ArkiLaunch

**Project slug:** `arkilaunch`
**Maintained by:** ArkiLaunch Team (Almara Construction capstone)
**Last updated:** 2026-09-19
**Built on FMD:** v1.28.1

---

> **Manifest / control panel.** Every doc, its version, status, and the two dates. Updated in the same turn any doc is touched. Product: ArkiLaunch, a multi-tenant SaaS for PH heavy-equipment rental MSMEs; Almara Construction is the pilot/anchor tenant. Scale: Full.

---

## 1. Document Suite

| Document | File | Version | Status | Last Updated | Last Reconciled |
|----------|------|---------|--------|--------------|-----------------|
| IDEA · Idea Brief | [idea-arkilaunch.md](idea-arkilaunch.md) | 0.1 | Draft | 2026-08-01 | N/A |
| VALIDATION · Validation Brief | [val-arkilaunch.md](val-arkilaunch.md) | 0.1 | Draft | 2026-08-01 | N/A |
| SCRUTINY · Scrutiny Gate | [scrutiny-arkilaunch.md](scrutiny-arkilaunch.md) | 0.1 | Draft | 2026-08-20 | N/A |
| VOICE · House Style | [voice-arkilaunch.md](voice-arkilaunch.md) | 0.1 | Draft | 2026-08-02 | N/A |
| PITCH · Pitch & Demo | [pitch-arkilaunch.md](pitch-arkilaunch.md) | 0.1 | Draft | 2026-08-01 | N/A |
| WRAP · Next Steps | [wrap-arkilaunch.md](wrap-arkilaunch.md) | 0.1 | Draft | 2026-08-01 | N/A |
| BRD · Business Requirements | [brd-arkilaunch.md](brd-arkilaunch.md) | 0.1 | Draft | 2026-07-25 | N/A |
| UES · Unit Economics Sheet | [ues-arkilaunch.md](ues-arkilaunch.md) | 0.1 | Draft | 2026-09-13 | N/A |
| PRD · Product Requirements | [prd-arkilaunch.md](prd-arkilaunch.md) | 0.1 | Locked | 2026-09-19 | 2026-09-19 |
| DSD · Design System | [dsd-arkilaunch.md](dsd-arkilaunch.md) | 0.1 | Locked | 2026-09-19 | 2026-09-19 |
| SDD · System Design | [sdd-arkilaunch.md](sdd-arkilaunch.md) | 0.1 | Locked | 2026-09-07 | 2026-09-07 |
| QAD · QA & Test Plan | [qad-arkilaunch.md](qad-arkilaunch.md) | 0.1 | Locked | 2026-09-07 | 2026-09-07 |
| SAD · Subagents | [sad-arkilaunch.md](sad-arkilaunch.md) | 0.1 | Draft | 2026-08-01 | N/A |
| BUILD · Build Guide | [build-arkilaunch.md](build-arkilaunch.md) | 0.1 | Draft | 2026-09-13 | N/A |
| CLR · Compliance & Legal | [clr-arkilaunch.md](clr-arkilaunch.md) | 0.1 | Draft | 2026-08-20 | N/A |
| AIA · AI Assurance Dossier | [aia-arkilaunch.md](aia-arkilaunch.md) | 0.1 | Draft | 2026-09-19 | N/A |
| GTM · Go-To-Market | [gtm-arkilaunch.md](gtm-arkilaunch.md) | 0.1 | Draft | 2026-08-01 | N/A |
| OPS · Ops & Observability | [ops-arkilaunch.md](ops-arkilaunch.md) | 0.1 | Draft | 2026-09-13 | N/A |
| LOG · Build Session Log | [log-arkilaunch.md](log-arkilaunch.md) | 0.1 | Draft (append-only) | 2026-09-07 | N/A |

### Runbooks (operational procedure, not suite docs)

| Runbook | File | Supports | Status |
|---------|------|----------|--------|
| Local development environment | [runbook-local-dev.md](runbook-local-dev.md) | BUILD §4 golden paths; `.env.example`; CR: frontend-contract-unblock | Draft |
| OCR fixtures + EDTR model training | [runbook-ocr-fixtures.md](runbook-ocr-fixtures.md) | RFC-002 §2/§5, QAD-T39, QAD §2 corpus floor | Draft |
| Remote Supabase deploy (migrate + seed) | [runbook-deploy-supabase.md](runbook-deploy-supabase.md) | BUILD §3 pinned stack; RFC-1 §3 platform tenant; `deploy.yml` migrate step | Draft |

### Audits and reports (findings, not suite docs)

An audit records what was found, not what was decided: these are evidence, not
authority, and nothing is built against them. Each finding closes through a
Change Record or is explicitly deferred there.

| Document | File | Scope | Status |
|----------|------|-------|--------|
| HTTP API surface gaps | [audit-api-surface.md](audit-api-surface.md) | `apps/api/src/**` controllers/services vs every path literal in `apps/web/src` | Findings (2026-09-19) |
| Database tenant isolation & data integrity | [audit-db-tenant-isolation.md](audit-db-tenant-isolation.md) | `packages/db` schema, RLS, migrations `0000..0016`, money-path callers | Findings (2026-09-19) |
| OCR to reconciliation to deduction | [audit-ocr-money-path.md](audit-ocr-money-path.md) | the money path end to end, against RFC-2's deduction rule | Findings (2026-09-19) |
| Docs vs code drift | [audit-docs-drift.md](audit-docs-drift.md) | `docs/*.md`, the four materialized artifacts, `.claude/agents/*.md` | Findings (2026-09-19) |
| Figma route alignment | [report-figma-route-alignment.md](report-figma-route-alignment.md) | frame-by-frame diff behind `cr-arkilaunch-figma-ia-alignment.md` | Report (2026-09-17) |

**Materialized at project root (not in `docs/`):** `BRAND.md`, `DESIGN.md` (from DSD §9), `AGENTS.md` (from BUILD §6), `MODEL_CARD.md` (from AIA §1). `README.md` is **not** a materialized artifact -- it is maintained by hand; the three files that used to disagree about this (this section, `CLAUDE.md` §Materialized-files, and README's own footer) were reconciled by `cr-arkilaunch-repo-cleanup-audit.md`. `AGENTS.md` §5.2 was re-materialized 2026-09-13 after sitting 13 months behind BUILD. `BRAND.md` and `MODEL_CARD.md` are current. `DESIGN.md` was **not**: it had been hand-edited away from the Locked DSD (build-status annotations, a `weather-stale` behavioural rule, and an admission at `:405` that it was hand-updated rather than regenerated), which this section asserted was not the case from 2026-09-13 until `cr-arkilaunch-doc-reconcile-2026-09-19.md` folded that content back into the DSD and re-materialized it.

### RFCs (one per major feature)

| RFC ID | File | Feature | Status | Last Updated |
|--------|------|---------|--------|--------------|
| arkilaunch-rfc-001 | [rfc-arkilaunch-tenancy-rls-auth.md](rfc-arkilaunch-tenancy-rls-auth.md) | Multi-tenant isolation + identity/auth (PRD-F7) | Locked | 2026-09-19 |
| arkilaunch-rfc-002 | [rfc-arkilaunch-ocr-edtr-reconciliation.md](rfc-arkilaunch-ocr-edtr-reconciliation.md) | OCR EDTR + double-entry reconciliation (PRD-F3, PRD-F6) | Locked | 2026-09-19 |
| arkilaunch-rfc-003 | [rfc-arkilaunch-quotation-pricing-engine.md](rfc-arkilaunch-quotation-pricing-engine.md) | Diesel-indexed dynamic quotation (PRD-F1) | Locked | 2026-08-02 |

### 1.1 Traceability Matrix

Must-Have `PRD-F#` coverage. Frozen in the PRD; never renumbered. A Must-Have with `no` in SDD or QAD is a gap.

| PRD-F# | Feature (short) | Priority | In SDD | In QAD | In RFC |
|--------|-----------------|----------|--------|--------|--------|
| PRD-F1 | Dynamic Quotation Engine | Must-Have | yes | yes | rfc-003 |
| PRD-F2 | PayMongo Payment Interface | Should-Have | yes | yes | N/A |
| PRD-F3 | OCR Usage-Based Billing + reconciliation | Must-Have | yes | yes | rfc-002 |
| PRD-F4 | Fleet Inventory, Maintenance & Reporting | Must-Have | yes | yes | N/A |
| PRD-F5 | Weather-Aware Module | Must-Have | yes | yes | N/A |
| PRD-F6 | OCR KYC & Registration | Must-Have | yes | yes | rfc-002 |
| PRD-F7 | Multi-Tenant Access, Identity & RBAC | Must-Have | yes | yes | rfc-001 |
| PRD-F8 | Client Booking Portal | Should-Have | yes | yes | N/A |

---

## 2. Change Log

Every material change to a Locked document is recorded as a Change Record. Newest first.

| CR ID | Date | Summary | Trigger doc | Docs touched | File |
|-------|------|---------|-------------|--------------|------|
| cr-arkilaunch-cart-validation | 2026-09-24 | Three defects on the customer path, reported together. **The cart had no validation**: every rule was one `disabled` prop on the submit button, so a dead button never said which field was at fault, and the rental dates were unchecked -- a cart persists in sessionStorage, so dates valid when the machine went in could be in the past at submit, which reached the API as a 400. It is a real `<form>` now, with `validateCart()` as a pure function beside it (company required and verified, site required, per-item dates not past / end after start / at most 365 days, contact and notes length-checked); errors stay quiet until the first submit then track every keystroke, and submitting focuses the first bad field. **Only a verified company can be selected** -- this supersedes `cr-arkilaunch-customer-prerequisites.md` §2's "booking before verification is allowed; payment is not" as a client-side gate, confirmed with the user against the concern that it blocks a new customer; the API's 409 `company_not_verified` contract is unchanged, unverified companies stay visible but disabled and labelled with why, and an account with nothing rentable gets an explanation instead of an unsubmittable form. **The sidebar blade marked the wrong page**: `/account` is a prefix of every account URL, so Home lit up on the cart, checkout, invoice and company form (`/app` likewise) -- `NavItem.exact` pins a section root, `NavItem.owns` lets "My bookings" claim checkout/invoices/negotiation which have no entry of their own, and matching is ranked by how much of the URL the matching prefix accounts for. **Migration 0028** adds `photo_uri` to both catalog SECURITY DEFINER readers, so an uploaded equipment photo finally renders anywhere -- the storefront had been using a three-entry hardcoded map of hotlinked stock images, now the fallback. Figma's serial number on the cart line was **declined**: `serial_no` is outside the public allowlist that 0027 restates, and `/catalog/equipment` is unauthenticated, so the line shows the short display code derived from the row id instead. Two migration traps recorded: `CREATE OR REPLACE` cannot add a column to `RETURNS TABLE` (42P13, so DROP/CREATE and re-issue the 0010/0012 grants or the function returns executable by PUBLIC), and a hand-authored journal entry whose `when` sits below the ledger watermark is skipped in silence while `db:migrate` still prints "Migrations applied". Deliberately not claimed: the gate is client-side only (the API still accepts an unverified booking), `MAX_RENTAL_DAYS` has no server counterpart, and the Playwright spec runs only in CI | user request 2026-09-24; Figma `ENpes2ZBsS3baRPKLyx0d3` Cart Page (`168:1982`) | index.md §2, cr-arkilaunch-customer-prerequisites.md §2 (superseded) | [cr-arkilaunch-cart-validation.md](cr-arkilaunch-cart-validation.md) |
| cr-arkilaunch-company-applications | 2026-09-23 | Makes the Figma "Manage Applications" screen (`251:1945`) work. `/account/applications` rendered one `Surface` from `GET /tenants/me/application`; the frame is a **list** -- Total/Approved/Pending counter tiles, search, three status tabs, and a card per company with a registration-document thumbnail, status pill, registration number and **Manage**, plus **Add New Company**. It was widely recorded as blocked on a missing list endpoint. It was not: it was reading the wrong entity. `GET /tenants/me/application` is the **tenant onboarding** application (a business becoming an ArkiLaunch tenant) and is correctly singular, because `users.tenantId` is a single FK and RLS keys off one tenant per JWT. What the frame draws is the companies a customer registers to rent under -- `customers` rows, plural since `cr-arkilaunch-customer-prerequisites.md`, already served by `GET /me/companies`, whose schema comment cites this Figma work by name. So `POST /me/companies` already was Add New Company and the documents screen already was a Manage target. Two things were genuinely absent: migration `0029` adds `customers.sec_number` (nullable; table-level grants from `0002` cover it, no paired grants migration), captured on the add-company form and prefilled from the `secNumber` suggestion `POST /me/kyc/scan` has always returned with nowhere to put it; and `GET /me/companies/:id/documents/:documentId/url` gives a customer a 300s signed URL for **their own** KYC document under `booking:read` -- the existing route is `quote:approve`, staff only. **RLS is not the gate on that read**: `customer` is an intra-tenant role, so every customer of a tenant shares the RLS scope and `ownsCustomer()` on top is what stops one customer pulling another's certificate by guessing a customer id (`audit-api-surface.md` #1); it refuses `not_found`, not `forbidden`, so no id is confirmed. `/account/companies` was a second list of the same rows and is **absorbed** -- it redirects, loses its sidebar entry, and its card becomes `/account/companies/$companyId`, what Manage opens. Counters and filtering are client-side over an unpaginated list, marked in place. Deliberately not claimed: no server-side filter or pagination, the admin-side gaps stay open (`/app/companies/approved` renders empty, the admin detail page `.find()`s page 0), and the Playwright spec is written but unrun -- the anchor seed refuses a non-local database and `dev` has no Playwright CI job | user request 2026-09-23; Figma `ENpes2ZBsS3baRPKLyx0d3` Manage Applications (`251:1945`) | index.md §2, report-figma-route-alignment.md §2/§5 | [cr-arkilaunch-company-applications.md](cr-arkilaunch-company-applications.md) |
| cr-arkilaunch-equipment-crud | 2026-09-23 | Builds the inventory CRUD surface the prototype draws over `/app/inventory` (Figma `292:1344`, `293:2668`, `293:3256`), which was a read-only grid. Corrects the premise first: `report-figma-route-alignment.md` claimed the API had no equipment write endpoint, but it had been checked against the `catalog` module -- `POST /equipment` and `PATCH /equipment/:id` have existed since `cr-arkilaunch-f4-f5-fleet-weather.md` with no UI ever calling them, and only `DELETE` was missing. **`DELETE /equipment/:id` is a retire, not a delete**: `edtr.equipment_id` is the evidence an invoice was computed from, so migration `0026` takes the rate-cards posture -- REVOKE UPDATE/DELETE on `equipment`, GRANT UPDATE back on exactly the eleven columns the request path writes -- which also makes `serial_no` immutable once a DTR cites the machine and `tenant_id` unreassignable. `0027` hides retired units from the two `@Public` catalog functions, closing a gap where a customer could have booked an out-of-service machine from the storefront. Adds the prototype's spec columns and one photo per machine on a **third, public-read Storage bucket** -- the one deliberate divergence, since SDD §7's 'never a public URL' is scoped to EDTR and KYC blobs and a machine photo is neither evidence nor personal data (§3). Also fixes an e2e suite that could not fail: `signIn` soft-skipped, so the authed console specs reported green having asserted nothing, and no CI job ran Playwright at all -- `signIn` now throws and a `console-e2e` job lands with it. Deliberately not claimed: the frame's HOURLY/DAILY RATE fields (rate_cards owns pricing; a second unwindowed price would be a competing source of truth on the money path), the `293:2913` delete mode, a second photo slot, and a real Supabase upload -- no live credentials in this environment, so the photo path is verified by contract and the reused validation layer rather than end to end | user-supplied Figma prototype `ENpes2ZBsS3baRPKLyx0d3` | sdd-arkilaunch.md §3/§4/§7, report-figma-route-alignment.md §2/§4, index.md §2 | [cr-arkilaunch-equipment-crud.md](cr-arkilaunch-equipment-crud.md) |
| cr-arkilaunch-customer-prerequisites | 2026-09-21 | Gives a real customer a way in. Before this, the storefront's "Create account" registered a rental business, customer logins existed only by staff invite with no `customers` row behind them (so `POST /bookings` 403'd for everyone but seeded users), and project sites were staff-only. Adds storefront **self-signup** into the anchor tenant through a SECURITY DEFINER `customer_register()` (migration `0023`; slug from server env, email unique across tenants), **several companies per login** with TIN, billing address, government ID and registration uploads, and **customer-owned project sites** pinned on a Leaflet/OSM map. Customer scope becomes "any company this login owns" across bookings, quotes and payments. **Payment is gated on staff verifying the company** (409 `company_not_verified`), while quotes stay open. Staff verify from the registration queues, which were placeholders. Deliberately not claimed: no email verification, no OCR of customer uploads, no edit/delete of companies or sites, no address geocoding, OSM public tiles at pilot volume only | user request; Figma `ENpes2ZBsS3baRPKLyx0d3` Add New Company | index.md §2 | [cr-arkilaunch-customer-prerequisites.md](cr-arkilaunch-customer-prerequisites.md) |
| cr-arkilaunch-scan-first-company | 2026-09-22 | Makes "Add New Company" scan-first. The Government ID and Company Registration captures are now **sequential** (never both on screen at once), and the manual form comes **last**, prefilled from the registration scan for final edits. Adds `POST /me/kyc/scan` (`booking:create`, 5/min): a typing aid that returns `companyName`/`tin`/`secNumber` suggestions and writes no `kyc_documents` row, makes no verification decision, and drops any value failing `TIN_REGEX`/`SEC_REGEX`; with no adapter available it returns nulls and the form opens empty. `KYC_QUERY_FIELDS` gains `CompanyName`. On the admin side, `POST /customers/:id/documents/:documentId/read` (`quote:approve`, 10/min, a reviewer's click rather than automatic on upload) saves `ocr_payload`/`format_valid`/`confidence` on the document, reports the **lowest** field confidence, shows malformed values as invalid rather than hiding them, and never sets `verified`; `PATCH /customers/:id/kyc` now takes the reviewer's corrected `companyName`/`tin`/`secNumber` and writes them on approval only. Liveness/selfie verification considered and declined (verifies the person not the company; RA 10173 biometric obligations). Supersedes "Uploaded documents are not OCR'd" in cr-arkilaunch-customer-prerequisites §4. Deliberately not claimed: a customer can now spend Azure DI pages (rate-limited, no per-tenant monthly cap), the scan stores nothing, `companyName` has no format check, and the government ID is not read at all | user request 2026-09-22 | index.md §2 | [cr-arkilaunch-scan-first-company.md](cr-arkilaunch-scan-first-company.md) |
| cr-arkilaunch-customer-journey | 2026-09-21 | Builds the customer journey between booking and payment, which did not exist: no quote was ever tied to a booking, negotiation was layout only, and checkout charged a flat deposit and never the rent. Adds `negotiation_messages` and `booking_change_requests` (migration `0022`, full RLS), customer quote accept/decline with a 7-day validity window, re-quoting that supersedes open quotes, and a checkout that charges the **accepted quote plus the contract deposit on one itemised invoice**, never an offer from the thread, never a deposit twice, never a paid booking again. Payment stays PayMongo hosted: the customer's method choice narrows the session's channels, so the GCash/bank login and OTP frames stay on PayMongo's page. Paid bookings are cancelled or extended by request; an approved extension re-runs the double-booking check. Customer notifications for each journey event, plus a staff `/app/bookings` screen to answer threads and requests. Deliberately not claimed: no realtime transport (10s polling), no email/SMS/push or staff notifications, no automatic refunds, delivery/return steps follow the calendar, channel codes unverified against current PayMongo docs, no live-stack e2e run | user-supplied Figma prototype `ENpes2ZBsS3baRPKLyx0d3` | index.md §2, cr-arkilaunch-figma-ia-alignment.md (negotiation note superseded) | [cr-arkilaunch-customer-journey.md](cr-arkilaunch-customer-journey.md) |
| cr-arkilaunch-viewfinder-capture | 2026-09-20 | Aligns the DTR/EDTR capture surfaces to the prototype's document-scanning design (Figma `OCR Tool`, `OCR Tool Review`, `Deployment-OCR Tool Selector`), and **partially supersedes `cr-arkilaunch-camera-capture-split.md` §2**: capture is now a real `getUserMedia` viewfinder with an alignment frame, shutter and torch, not two buttons over a file input. That earlier record's conclusion is kept where it was right -- the OS file picker stays a separately-labelled action -- and the fallback to it is load-bearing, since `getUserMedia` is undefined on an insecure origin (which `http://<lan-ip>` for phone testing is) and rejects on denied permission or no camera. Two defects found while tracing the path are the substantive part. **With `ENABLE_OCR_PIPELINE=true` every paper capture failed**: the flag is a server-side env read, the web client had no way to ask, so it always sent transcribed hours and `edtr.service.ts` always answered 422 `line_items_not_accepted` -- the screen was unusable in the one configuration the OCR path exists for. Fixed by a new `GET /reference/capabilities` rather than a second `VITE_*` copy of the flag, which would drift silently. **And `bounding_region` was a schema field nothing ever wrote**: carried in `OcrFieldSchema` since RFC-2 §3 and mapped into every detail response, but absent from the port types, never read off the Azure response and never set by the worker, so it was always null; a review overlay built against it would have drawn nothing. Now plumbed end to end, with the polygon **normalised to 0..1 against its own page** in the adapter, because Azure reports inches for a PDF and pixels for an image and a raw-coordinate consumer would be right for one and wrong for the other with no symptom but a box over the wrong cell. A malformed polygon or a dimensionless page yields no box rather than a guess. Also adds `GET /edtr/:id/image` (300s signed URL, key re-derived from the owning row under RLS, never client-supplied, 404 not 403 across tenants), a review overlay colouring each reading by the 0.90 gate with a text legend, a deployment-first `/app/ocr/deployments` + `/field/scan` entry so a sheet is scoped before the camera opens, and a `useScanDeployments` hook replacing the duplicated reference-loading in two routes. Nothing on the review screen deducts; corrections still pass the RFC-2 §3 gate. Deliberately not claimed: no edge detection or auto-capture (the frame is a static guide, marked in source with the OpenCV.js upgrade path); the deployment row is a rental, since the mock's operator, driver and contract value have no backing table and closing that is a schema change; the Session Data panel shows the machine, rental and day rather than the mock's invented asset/zone/resolution; real-device camera behaviour is unverified in CI, as jsdom can assert the fallback and a stubbed shutter but not what an Android browser does with `facingMode` or `torch`; only `hours_active` currently carries a polygon, and KYC's `queryFields` path carries none; and no extraction-accuracy claim is made, so AIA-R5 is unchanged | build-arkilaunch.md §5.1 | cr-arkilaunch-camera-capture-split.md §2 (superseded in part), index.md §2/§5 | [cr-arkilaunch-viewfinder-capture.md](cr-arkilaunch-viewfinder-capture.md) |
| cr-arkilaunch-doc-reconcile-2026-09-19 | 2026-09-19 | Doc-reconcile pass triggered by the full-system audit's docs-vs-code sweep. Registers the six documents the manifest did not list -- the four `audit-*.md` findings docs, `report-figma-route-alignment.md`, and, the serious one, **`cr-arkilaunch-edtr-real-form.md`, a Change Record for shipped work that had no §2 row at all**. Corrects this file's own counts (§5 said 44 `docs/*.md` files against 53 on disk; README said 42), five stale `Last Updated` cells, and its header date -- the same dead-dates class `cr-arkilaunch-repo-cleanup-audit.md` already fixed once on 2026-09-13, recorded as a recurrence rather than as news, since nothing in this repo checks it. **Folds the hand-edited `DESIGN.md` back into the Locked DSD** rather than stripping it: the five `**Built:**` annotations and the `weather-stale` rule describe real shipped behaviour, so they are written into the DSD and the artifact re-materialized from it, restoring the rule DSD §9 states and that §1 here had been falsely claiming held. Reconciles three Locked docs that describe a system which does not exist: RFC-1's frozen permission catalog (seven codes, none of which exist in `PERMISSION_CODES`, and which would not compile against the `PermissionCode` union), its `owner` "explicitly lacks any `*:manage`" row against a seed that grants two, and its stale `/app/kyc` path; PRD §5.2's two non-existent checkout routes; and the DSD font spec that contradicted both its own artifact and `index.html`. Drops **breakdown status** from PRD §8 / SDD §5 / AIA §1.2 and re-materializes `MODEL_CARD.md`: nothing extracts it, and the claim was inflating the AI assurance dossier. Narrows RFC-2's EDTR extraction description from `queryFields` to the `prebuilt-layout` tables that shipped, keeping `queryFields` for KYC where it is still correct. **Back-fills three shipped changes that never got a record** -- the PR #26 pagination wire-contract change across four live endpoints, the PR #24 toast/modal/confirm-dialog layer plus label rewrite, and the `79c432e` rebuild of S4 Dashboard -- as entries in this record rather than three new files, per the 2026-08-20 combined-record precedent. Deliberately not claimed: SDD §4's endpoint contracts are not rewritten for the paged shape, DSD §6 gains no component specs for the new primitives, the `audit-api-surface.md` #11/#12 feature backlog stays unbuilt, and no automated drift check is added -- this class has now been caught by hand three times, which is recorded in §4 as a process finding rather than papered over | audit-docs-drift.md | index.md §1/§2/§4/§5, dsd-arkilaunch.md §2/§5/§8, DESIGN.md, rfc-arkilaunch-tenancy-rls-auth.md §3, rfc-arkilaunch-ocr-edtr-reconciliation.md §2, prd-arkilaunch.md §5.2/§8, aia-arkilaunch.md §1.2, MODEL_CARD.md, README.md, the four audit-*.md | [cr-arkilaunch-doc-reconcile-2026-09-19.md](cr-arkilaunch-doc-reconcile-2026-09-19.md) |
| cr-arkilaunch-edtr-real-form | 2026-09-18 | **Registered retroactively by `cr-arkilaunch-doc-reconcile-2026-09-19.md`: this row was missing, so a Change Record for shipped work sat outside the change log.** Records the refactor that made EDTR extraction work against the real Almara multi-day timesheet: the pivot from `queryFields` to `prebuilt-layout` table extraction after the letterhead came back at 0.883 confidence and the form's actual grid structure did not match the field schema the codebase assumed; a table-grid extension to the document-intelligence port and the Azure adapter; an `edtr-sheet` parser that fans one captured sheet out into one row per dated line; merged-cell expansion after the first real form dropped its merged headers; and per-cell confidence derived from constituent word spans. Also makes `hours_idle` nullable end to end (migration `0017`, reconciliation gate, API, UI), because the real paper form has no idle column at all and writing 0 would hand the deduction gate a fabricated reading | build-arkilaunch.md §5.1 | rfc-arkilaunch-ocr-edtr-reconciliation.md (addendum), packages/document-intelligence, packages/db, jobs, apps/api, apps/web | [cr-arkilaunch-edtr-real-form.md](cr-arkilaunch-edtr-real-form.md) |
| cr-arkilaunch-figma-ia-alignment | 2026-09-17 | Reconciles the **PRD §5.2 IA tree** with the routes that actually shipped, closing the obligation `cr-arkilaunch-frontend-storefront-shell.md` §5 left open ('a future CR should reconcile the PRD §5.2 IA tree itself if this pass's `/account/*` branch is kept'). It was kept. Three information architectures had been running in parallel with no authority between them: the Locked PRD tree, the shipped router, and a Figma prototype. Diffing them found that **§5.2 has been describing routes that do not exist since 2026-08-02** -- `/app/fleet`, `/app/edtr`, `/app/sites`, `/app/reports`, `/app/billing`, `/app/kyc` and `/app/bookings` were all renamed or moved in the storefront-shell pass and the Locked table was never updated. §5.2's destination table and IA tree are amended to the shipped names; the frozen S1-S25 inventory in §5.1 is **not** renumbered. Also records that the prototype is a **different Figma file** from the one the previous CR read (`kqLTdB0RjduogSqpjB8uKr`, ~180 frames, versus `7rUbrJxQpw0nNRWegoQRtZ`, ~120), so that CR's frame ids no longer resolve. The full frame-by-frame diff is in `report-figma-route-alignment.md`. Adds the prototype screens that were never built: the registration split (six states the code lumped into two routes), the checkout and invoice surfaces, rental lifecycle, notifications, profiles, tickets, security logs, and the negotiation screens as layout only. **Rejects, rather than defers, the prototype's in-app payment credential capture** (GCash Authentication, OTP Verification): DSD §4.1 (Locked) forbids collecting card data in-app and the storefront-shell CR already superseded it with PayMongo hosted checkout, so only the PayMongo-shaped pre/post screens are built -- confirmed with the user before any code was written. Deliberately not claimed: `/app/security-logs`, `/app/tickets`, the three notification routes and the negotiation screens have **no backend of any kind** and are layout against placeholder data, each needing its own CR and an SDD §5 API addition before it means anything; the 420px mobile frames are honoured as responsive breakpoints rather than separately built screens; and the prototype's own palette is not adopted, every new page rendering in Yardboard tokens | user-supplied Figma prototype `kqLTdB0RjduogSqpjB8uKr` | prd-arkilaunch.md §5.2, index.md §2, report-figma-route-alignment.md | [cr-arkilaunch-figma-ia-alignment.md](cr-arkilaunch-figma-ia-alignment.md) |
| cr-arkilaunch-ocr-extraction-enablement | 2026-09-16 | Turns on **real Azure DI extraction for KYC** and records, as verified fact rather than prediction, that **the EDTR pipeline cannot be turned on yet**. Checked against the live `di-arkilaunch-dev` resource: `prebuilt-layout` with `features=queryFields` returned a SEC number and TIN at **0.995** confidence from a synthetic document, which also settles an open question the docs never answered, that **`queryFields` is available on the F0 tier** (a premium add-on the free tier might plausibly have refused, which would have forced the KYC design back to the drawing board). The response shape matched the adapter's existing mapping exactly, so KYC needed no code change and was only ever waiting on configuration. By contrast `arkilaunch-edtr-neural-v1` returns **404** and the resource carries no custom model of any kind, confirming the note `model-registry.ts` and `cr-arkilaunch-pilot-honesty.md` §4 have carried all along. `ENABLE_OCR_PIPELINE` is therefore deliberately left `false`: with it on, the API rejects the transcribed hours the pilot's only approvable deduction path depends on, queues the row, and the worker 404s it to `hard_failed`, which is an outage with extra steps rather than extraction. Enabling it needs a labeled corpus, a training run, and `EDTR_REQUIRED_FIELDS` re-derived from the trained model's real output keys, in that order. Ships one test change: the adapter's specs asserted response mapping against hand-written payloads and had never met a real endpoint, so the outgoing request is now pinned to the shape the live resource accepted, including the `features=queryFields` pairing Azure rejects `queryFields` without. Scope of the enablement is one developer's local `.env`; **no Terraform variable, GitHub environment or deployed environment changed, and both flags remain `false` in dev and prod**. Claims nothing about compliance: AIA-R7 stays Open (escalated) with its §4 launch pause in force, since the resource is in Singapore and a PH-origin document still crosses a border; CLR E1/E2/E4/E11 all still block a real KYC launch; the synthetic test document held no real personal data. Deliberately not claimed: the full API path (signed URL to download to analyze to `needs_review` row) was **not** run end to end, since restarting the local API was blocked by a tool permission, so it is recorded as the next thing to verify; F0 is rate limited and will not support a pilot; 0.995 on a machine-generated PDF says nothing about a photographed certificate; and no golden-set accuracy number is claimed for either flow | build-arkilaunch.md §5.1 | aia-arkilaunch.md §2, rfc-arkilaunch-ocr-edtr-reconciliation.md §7, index.md §2 | [cr-arkilaunch-ocr-extraction-enablement.md](cr-arkilaunch-ocr-extraction-enablement.md) |
| cr-arkilaunch-camera-capture-split | 2026-09-16 | Splits **taking a photo from choosing a file** on both OCR intake screens, and lands the client-side compression RFC-2 §2 and SDD §4 have specified since 2026-07-25 and which was never built. Both screens rendered one `<input type="file">` carrying `capture="environment"`, which on most mobile browsers does not offer the camera alongside the picker but **replaces the picker with the camera**: a timekeeper could not attach a sheet already photographed, and a KYC operator could not attach a PDF that `POST /kyc/extract` plainly accepts. A new `CaptureField` renders two native inputs, one with the attribute and one without, behind two buttons, deliberately staying on the platform file input (restraint ladder rung 4) rather than building a `getUserMedia` viewfinder. The compression gap was survivable while intake was a deliberate file choice and expensive the moment it is camera-first: a phone photo is 3 to 12MB and often HEIC, and both server refusals (413, 422 `unsupported_or_forged_content_type`) arrived only after the whole file had crossed a 3 to 5 Mbps link. Capture now decodes with the EXIF orientation applied (a sideways photo is no longer stored rotated), caps the long edge at 2200px, re-encodes to JPEG, and takes one more downscale rung before refusing locally at no bandwidth cost. **A second, larger defect surfaced while testing the first on a real phone: the timekeeper could not reach EDTR capture at all.** `/app/ocr` is the only screen that opens the capture modal and `_app.tsx` guards it to admin/owner/platform_admin, so the role PRD US-02 is written for was redirected to `/field` and found no way to record anything, leaving S21 without the intake PRD §5.1 specifies. The modal moves to a shared component and `/field` gains a 'Record a field log' action; the server had always allowed it, since `POST /edtr` requires `edtr:create`, which that role holds. Also fixes an object URL leaked on every preview since the screens were written. Server-side validation is unchanged and stays authoritative; the mirrored client caps are labelled as a courtesy, not a control, on both sides. Deliberately not claimed: US-02 AC3 is only **partially** met (no upload progress indicator, no retry, no offline queue, no chunked/resumable transfer, all still specified and still absent); the 2200px/q0.82 setting is a judgement call unvalidated against the golden set, so no accuracy claim is made and AIA-R5 stays Open; and the real mobile behaviour is unverified here, since jsdom can only assert which input carries the attribute, not what a given Android browser does with it | build-arkilaunch.md §5.1 | rfc-arkilaunch-ocr-edtr-reconciliation.md header/§2, prd-arkilaunch.md header/US-02/§5.1, index.md §1/§2 | [cr-arkilaunch-camera-capture-split.md](cr-arkilaunch-camera-capture-split.md) |
| cr-arkilaunch-repo-cleanup-audit | 2026-09-13 | Full-repo cleanup audit for stale docs, dead code and config rot. The finding that carries the pass is not a docs defect: the **Docker runtime stage never copied `packages/weather`**, so `jobs/node_modules/@arkilaunch/weather` landed in the image as a dangling symlink and the dev `weather_poll_job` cron (enabled in `dev/terraform.tfvars`) died on `ERR_MODULE_NOT_FOUND` every tick since 2026-08-20 -- the same omission the Dockerfile's own comment warns about, fixed in the deps and build stages but not the runtime one. `README.md` claimed `F3/F1/F5/F6/F4/F8/F2 are not yet built` when all eight backends shipped 2026-08-02, so it is rewritten to state the real position in both directions (eight backends real; frontend a shell, deploy unproven, OCR never run on a real document, two CRs open) and to settle that README is hand-maintained, not materialized -- an ambiguity §1 here, `CLAUDE.md` and README's own footer each answered differently. `AGENTS.md` §5.2 was 13 months behind BUILD §5.2, and auditing which was right showed BUILD was overclaiming: `prerender.mjs` exists but `apps/web/vercel.json`'s `buildCommand` never invokes it, so two crawlability boxes are now `[~]` in both files and two stay `[x]` because they hold without it. Also: the `scripts/materialize.py` banner credit (no such file), `.gitignore` entries for two `.claude/` files protected only by machine-local rules, the root `build` script silently skipping the `jobs` workspace, a `.vercelignore` that is inert at best and lockfile-breaking at worst, two undocumented env vars, one unreferenced component and four zero-call-site query factories, and this registry's own drift (16 dead `Last Updated` dates, an unregistered runbook, a doc count of 23 against 43 files). Recorded, not fixed: the prerender wiring (needs a Playwright download in the Vercel build), `cross-tenant-isolation-suite` possibly running in no job at all if `vars.RUN_LIVE_DB_TESTS` is unset, a direct push to `dev` deploying without CI, the `newman-api-suite` echo stub, ~19 unused shared/db type exports, orphaned Drizzle snapshots, and `express` as a phantom dep | build-arkilaunch.md §5.1 | index.md §1/§2/§5, build-arkilaunch.md §5.2, log-arkilaunch.md §1, README.md, AGENTS.md, BRAND.md, DESIGN.md | [cr-arkilaunch-repo-cleanup-audit.md](cr-arkilaunch-repo-cleanup-audit.md) |
| cr-arkilaunch-deploy-unblock | 2026-09-13 | Unblocks **PRD §9 M5 (anchor pilot deployment)**, which could not start: every `Deploy` run since 2026-08-06 died at `azure/login` in about twelve seconds, and `deploy.yml` had no migrate step at all. Root cause was not a bug in the YAML. The Entra app registration and OIDC federated credential existed only as prose in `infra/terraform/bootstrap/README.md` §3, described there as a manual step outside Terraform, so nothing ever created it and the `client-id`/`tenant-id`/`subscription-id` inputs resolved to empty strings. Worse, the subjects §3 prescribed (`repo:<org>/<repo>:ref:refs/heads/dev`) could never have matched: both jobs declare `environment:`, so GitHub issues the token with an `environment:` subject claim, meaning anyone who had followed the runbook exactly would still have been stuck, with a more confusing symptom. Declares the application, service principal, Contributor assignment and both federated credentials in the bootstrap Terraform, and corrects §3. Adds the migrate step between the image push and `terraform apply` (that order is load-bearing: apply rolls the API revision and the four cron jobs onto the new image, so migrating after it would leave new code reading a schema that is not there yet), passing `DATABASE_URL_DIRECT` rather than the transaction-mode pooled URL, and `APP_AUTHENTICATED_PASSWORD`, which `migrate.ts` only warns about and whose absence would leave the step green while every pooled runtime connection failed. Gates `deploy` on `terraform-plan` per the Azure DI record §6 item 7, which required plan to stop being `pull_request`-only since a skipped job blocks its dependents and the two guards were mutually exclusive. The identity was first written as an Entra app registration and that failed halfway against the shared tenant that owns this subscription: the app was created, then the service principal and both credentials returned 403, because the `azuread` provider creates an application ownerless and that directory lets a non-admin create an app registration but not own, modify or delete one. Replaced with a user-assigned managed identity, which carries the same federated credential under subscription RBAC with no directory privilege and no change to `deploy.yml`. Bootstrap applied 2026-09-13 (5 added, 0 destroyed) and all four variables set on both GitHub environments. Deliberately not claimed: no `Deploy` run has yet proven `azure/login` succeeds, so the pipeline is unproven rather than fixed until the first push to `dev` goes green; an ownerless `arkilaunch-github-deploy` app is stranded in a tenant we cannot clean up and needs a directory admin there; the `terraform-plan` fork exposure (a PR can obtain Azure Contributor via `environment: dev`) is recorded, not fixed; Contributor is subscription-wide in a subscription that hosts other people's work; and `0016` turned out to be already live on dev, applied by hand on 2026-08-07, so the step prevents future drift rather than repairing a stale database | build-arkilaunch.md §5.1, cr-arkilaunch-azure-di-provisioning.md §6 | index.md §2/§4 | [cr-arkilaunch-deploy-unblock.md](cr-arkilaunch-deploy-unblock.md) |
| cr-arkilaunch-deduction-rate-card-effectiveness | 2026-09-13 | Closes a money-path defect found while unblocking M5: `EdtrService.approve()` resolved the hourly rate for a deposit deduction with `order by effective_from desc limit 1` and **no effectiveness filter at all**, so a superseded or not-yet-in-force card could price a real deduction. RFC-3/QAD-T44/T48 guard exactly this on the quotation side (`pricing-engine.service.ts:158-166`); the side that deducts from a customer's deposit did not, while its own comment claimed the card was "currently-effective" and that it "reuses the same rate-card lookup the quotation engine uses". Now filters `effective_from`/`effective_to` and prices against the EDTR's `report_date` rather than `now`, since the work happened then and a later rate change cannot retroactively reprice it. The new filter creates a state that could not arise before (cards exist, none covers the date) which would have fallen through to the pre-existing 0 rate and posted a zero-value deduction reading as a real approved one, so that fails closed with 422 `rate_card_not_effective`. Two further defects came from the `edtr-ocr-worker` gate, which returned FAIL first: anchoring `report_date` to midnight UTC put the cutoff at 08:00 Manila and would have blocked correct same-day approvals (a regression this pass introduced, now compared as Manila calendar dates), and the lookup never filtered `rate_type` although it multiplies by hours, so a daily card could price as hourly at 24x and the `order by` was non-deterministic. Also explains the four `billing-engine.spec.ts` tests red on `dev` since 2026-09-07: `quotes-engine.spec.ts` leaves a permanent 999999/hr card on the shared test project, three of the four failures cascading from the two blocked approvals. All six now pass. Recorded, not fixed: same-day deactivation now fails closed for that whole Manila day, intra-day rate changes are not expressible, `report_date` is now a client-controlled price-selection input bounded only by the RFC-2 human gate, and an equipment type with no rate cards at all can still deduct at 0 | build-arkilaunch.md §5.1 | index.md §2 | [cr-arkilaunch-deduction-rate-card-effectiveness.md](cr-arkilaunch-deduction-rate-card-effectiveness.md) |
| cr-arkilaunch-m4-money-path-gates | 2026-09-07 | PRD §9 **M4 (Testing & QA) iteration 1**. Closes the reconciliation false-accept that M4's own risk column names: `reconcileEdtr()` compared one summed `hours_active + hours_idle` scalar per log, so an 8h-active/0h-idle sheet matched a 0h-active/8h-idle counterpart at `delta_hours = 0` while `approve()` prices active hours alone — two logs disagreeing about every billable hour on the sheet, reported as perfect agreement. `evaluateGate()` now requires active, idle **and** the summed total to sit within tolerance (the total kept deliberately: dropping it would have made the gate looser than the old one for same-signed errors), and `delta_hours` stores the worst dimension via a `worstDelta()` shared with the gate. Closes the `tenants`/`equipment_types`/`subscription_plans` grant hole (migration `0016`): INSERT on `tenants` was still live, self-approving a tenant past the PRD-F6 KYC gate in `tenants_register()`; both global catalogues were rewritable by any authenticated tenant; and `tenants` had no RLS, so the platform's whole customer list was cross-tenant readable. Turns `money-path-e2e` and `ocr-accuracy-gate` from `echo` stubs into jobs that run, giving QAD-T26/T40 CI enforcement for the first time. Three adjacent defects fixed: a log with zero line items auto-accepted on no evidence; `approve()` erased the machine's `reason` when a human supplied adjustments; and `delta_hours`' definition had drifted across SDD §3, RFC-2 §4 and PRD §5.6. All five pre-merge subagents run and recorded with real verdicts (one FAIL, fixed) — the first CR to fill that table rather than leave it `*pending*`. Deliberately not claimed: `ocr-accuracy-gate` does not assert >= 90.06% (synthetic golden set at 6/8, model untrained), no QAD §6 box is ticked, and the DB-backed suites could not be executed locally (no Docker, no local Postgres; `.env` points at live pilot data) so they are verified by CI on the PR | build-arkilaunch.md §5.1 | rfc-arkilaunch-ocr-edtr-reconciliation.md header/§2/§4, qad-arkilaunch.md header/§4/§6, sdd-arkilaunch.md header/§3/§4, prd-arkilaunch.md header/§5.6, index.md §1/§2/§4 | [cr-arkilaunch-m4-money-path-gates.md](cr-arkilaunch-m4-money-path-gates.md) |
| cr-arkilaunch-doc-reconcile-2026-08-20 | 2026-08-20 | Doc-reconcile pass: `cr-arkilaunch-pilot-honesty.md` (2026-08-13) named RFC-2 §2/§3/§7, QAD §2/§4, and AIA as touched, but `manual_transcription` -- the CR's central mechanism and the only way a deposit deduction currently gets approved -- was never written into any of the three, so Locked RFC-2 still described the pre-pilot-honesty capture/reconciliation behavior. Writes back the missing addenda (manual-transcription capture path, the summed-hours-only reconciliation gap vs the five-dimension spec, the untrained `arkilaunch-edtr-neural-v1` model, the `dev`-as-staging and Newman-vs-supertest QAD deviations, and the AIA-R1/R3/R5 "not exercised" status note), then corrects `index.md` §4's Health Check, which had asserted `[x]` for both "Last Reconciled dates lead the code" and "no open Change Records" while three CRs sat unreconciled and two CRs were closed with unfilled verification/gate sections. Also flags, without fixing (out of scope for a docs-only pass): the reconciliation summed-hours blind spot as a live code gap, the three `echo`-stub CI jobs (`ocr-accuracy-gate`/`newman-api-suite`/`money-path-e2e`), and a `tenants`/`equipment_types`/`subscription_plans` RLS grant hole found in the same review | index.md §4 (Health Check) | rfc-arkilaunch-ocr-edtr-reconciliation.md header/§2, qad-arkilaunch.md header/§2/§4, aia-arkilaunch.md §2, prd-arkilaunch.md header, sdd-arkilaunch.md header, index.md §1/§2/§4 | [cr-arkilaunch-doc-reconcile-2026-08-20.md](cr-arkilaunch-doc-reconcile-2026-08-20.md) |
| cr-arkilaunch-open-meteo-free-tier | 2026-08-20 | Lands the real Open-Meteo HTTP adapter (`@arkilaunch/weather`, native fetch, Zod-validated response so a missing/mis-scaled field can never render a fabricated all-clear) that PRD-F5 was missing end-to-end, against the FREE tier rather than the commercial plan the PRD names -- a deliberate, recorded divergence: cost/quota under SCRUTINY G-4 are resolved (PHP 0; 10,000 calls/day cap enforced by a per-cycle site ceiling that aborts the whole cycle rather than exceeding it), but the free tier's non-commercial-use restriction is carried forward as G-4's one remaining open item, not resolved. CC BY 4.0 attribution shipped in `WeatherBanner`. Also: `createWeatherAdapter()` finally satisfies the eslint message that had promised it since Azure DI's factory landed; `UnavailableWeatherAdapter`'s default reason corrected from `'no_credentials'` (now impossible on a keyless API) to `'no_adapter'`; and `packages/document-intelligence`'s test suite, silently absent from every CI run since 2026-08-15, is added alongside the new package's | build-arkilaunch.md §5.1 | prd-arkilaunch.md §8, sdd-arkilaunch.md §2/§4/§5/§6/§7, qad-arkilaunch.md §2/§4 (QAD-T17/T31), scrutiny-arkilaunch.md §1/§3 (FC-7/G-4), ues-arkilaunch.md (UES-F4), ops-arkilaunch.md §2/§3/§4 (SLO-12/A7/runbook 4.3), clr-arkilaunch.md §1, build-arkilaunch.md §3, index.md §5 | [cr-arkilaunch-open-meteo-free-tier.md](cr-arkilaunch-open-meteo-free-tier.md) |
| cr-arkilaunch-azure-di-provisioning | 2026-08-15 | Provisions the first real Azure AI Document Intelligence resource as Terraform-managed infrastructure (southeastasia, `kind = FormRecognizer`, F0 in dev as a hard spend ceiling, S0 in prod which bills nothing while the flags are off) and lands the real `DocumentIntelligencePort` adapter that `cr-arkilaunch-pilot-honesty.md` §8 deferred; also wires Azure Monitor OpenTelemetry into the API and all 4 jobs so `APPLICATIONINSIGHTS_CONNECTION_STRING` (provisioned since the original Terraform stack but never consumed) finally reaches App Insights. Re-arms **AIA-R7** (still Open, escalated) exactly as the pilot-honesty record said it would; both OCR flags stay `false` in dev and prod, so no PH personal data has crossed a border. Also fixes three latent bugs the real adapter would have activated silently: the EDTR worker had no `ENABLE_OCR_PIPELINE` gate at all, a missing extracted field defaulted to zero hours via `?? 0`, and `analyze()` ran inside a Postgres transaction | build-arkilaunch.md §5.1 | aia-arkilaunch.md, clr-arkilaunch.md §1, rfc-arkilaunch-ocr-edtr-reconciliation.md §5/§7, ops-arkilaunch.md §2/§4, index.md §2 | [cr-arkilaunch-azure-di-provisioning.md](cr-arkilaunch-azure-di-provisioning.md) |
| cr-arkilaunch-pilot-honesty | 2026-08-13 | Pilot-readiness pass ahead of the Almara production pilot, with no vendor credentials available: makes every extraction port fail closed so a stub can never fabricate a value (the KYC module bound a literal `CS202312345` at 0.95 confidence in production code, and the weather stub's all-zero reading rendered a false all-clear for a construction site); unblocks the deduction path, which could never have approved a single deduction because reconciliation requires one `paper_ocr` plus one `digital_entry` and no OCR meant no pair could form, by letting a photographed paper sheet carry human-transcribed hours as `model_id: 'manual_transcription'`; and puts the 33 already-written spec files that ran in zero CI jobs behind a real gate | build-arkilaunch.md §5.1 | rfc-arkilaunch-ocr-edtr-reconciliation.md §2/§3/§7, qad-arkilaunch.md §2/§4, aia-arkilaunch.md, index.md §2 | [cr-arkilaunch-pilot-honesty.md](cr-arkilaunch-pilot-honesty.md) |
| cr-arkilaunch-frontend-deploy-readiness | 2026-08-07 | Frontend deploy-readiness pass: fixes the Terraform API output (was the revision-specific FQDN), adds a root `vercel.json` for the pnpm monorepo, fixes the RFC-1 §3 access-token-in-memory drift, finishes the 2FA login flow, replaces the raw-JSON DataPanel placeholders with a real table, and wires app.users/account.settings/account.applications/app.settings/a new platform-applications console screen/account.cart onto the backend surface the three 2026-08-06 CRs unblocked | build-arkilaunch.md §5.1 | index.md §2 | [cr-arkilaunch-frontend-deploy-readiness.md](cr-arkilaunch-frontend-deploy-readiness.md) |
| cr-arkilaunch-frontend-contract-unblock | 2026-08-06 | Backend-unblock pass for frontend work: local dev runbook, shared response Zod schemas for the read surface (sites/billing/bookings/notifications/fleet reports), `GET /tenants/applications`, `GET /tenants/me/application`, `GET /users/me`, `GET /catalog/equipment/:id`, and a fixture KYC extractor replacing the always-empty stub | build-arkilaunch.md §5.1 | sdd-arkilaunch.md §4 | [cr-arkilaunch-frontend-contract-unblock.md](cr-arkilaunch-frontend-contract-unblock.md) |
| cr-arkilaunch-storage-upload | 2026-08-06 | Implements RFC-2 §6 as written for the first time: EDTR/KYC uploads go multipart through the API (content-type allowlist, magic-byte sniff, decompression-bomb guard) then to Supabase Storage, replacing the base64-in-JSON POC hack; signed URLs kept for reads only, never for the upload itself | build-arkilaunch.md §5.1 | rfc-arkilaunch-ocr-edtr-reconciliation.md §6 | [cr-arkilaunch-storage-upload.md](cr-arkilaunch-storage-upload.md) |
| cr-arkilaunch-tenant-registration-catalog | 2026-08-06 | Backend-unblock pass: `POST /tenants/register` + approval workflow (new `tenant_applications` table, `tenant:approve` permission), an anchor-tenant public `GET /catalog/equipment`, and an admin-initiated `POST /users/:id/reset-password` reusing the invite/activate machinery | build-arkilaunch.md §5.1 | sdd-arkilaunch.md §3/§4, rfc-arkilaunch-tenancy-rls-auth.md §3 | [cr-arkilaunch-tenant-registration-catalog.md](cr-arkilaunch-tenant-registration-catalog.md) |
| cr-frontend-storefront-shell | 2026-08-02 | Builds Almara's storefront landing page, auth flow, and role-aware app shells reconciled against a user-supplied Figma prototype (which turned out to describe a single-tenant Almara storefront plus features absent from the frozen PRD); adds a Playwright build-time prerender for public routes closing the BUILD §5.2 crawlability checklist; logs the Figma-only features (negotiation flow, in-app payment capture, Ticket Management, Security Logs, Extend Rental, a distinct Operator role, a public catalog) as a deferred backlog | user-supplied Figma prototype | sdd-arkilaunch.md §2/§6, build-arkilaunch.md §5.2, voice-arkilaunch.md §2 | [cr-arkilaunch-frontend-storefront-shell.md](cr-arkilaunch-frontend-storefront-shell.md) |
| cr-dsd-marketing-tier | 2026-08-02 | Merges a SprintForge-derived marketing visual system into the DSD as a new, explicitly scoped "Marketing" tier (S1/S22 only), keeping ArkiLaunch's amber/steel/IBM Plex identity and the Console tier's instrument-panel rules (tight radii, no backdrop-filter, <=250ms motion) fully intact everywhere else; also lands the Console token layer and primitives in code for the first time (`apps/web/src/index.css`, `apps/web/src/components/`) | user request (design system merge) | dsd-arkilaunch.md §0/§2 to §8, prd/sdd/qad/rfc-001/002/003 Status headers (drift fix), build-arkilaunch.md §1 | [cr-arkilaunch-dsd-marketing-tier.md](cr-arkilaunch-dsd-marketing-tier.md) |
| cr-f9-read-surface | 2026-08-02 | Backend read-surface completion: new billing/notifications modules, EDTR review queue + reject, sites CRUD/deployments/weather-advisories/incidents, financial report, global rate limiting + login lockout, and a deposit-ledger arithmetic fix (approve() now caps deductions at `rental_contracts.deposit_required` instead of only ever growing) | build-arkilaunch.md §5.1 | sdd-arkilaunch.md §4, rfc-arkilaunch-tenancy-rls-auth.md §3 | [cr-arkilaunch-f9-read-surface.md](cr-arkilaunch-f9-read-surface.md) |
| cr-f2-f8-bookings-payments | 2026-08-02 | PRD-F2/F8 backend implementation: authenticated `customer` role (not public/guest) for bookings, a booking is `rentals`+`equipment_assignments` (no new table), real PayMongo checkout/webhook adapter behind `ENABLE_PAYMENTS`, webhook event names corrected against live docs, SECURITY DEFINER tenant lookup for the webhook | build-arkilaunch.md §5.1 | sdd-arkilaunch.md §4, rfc-arkilaunch-tenancy-rls-auth.md §3 | [cr-arkilaunch-f2-f8-bookings-payments.md](cr-arkilaunch-f2-f8-bookings-payments.md) |
| cr-edtr-double-approve | 2026-08-02 | Fixes the double-deduction finding deferred by cr-f4-f5-fleet-weather: `EdtrService.approve()` now locks and jointly transitions both reconciliation rows of a matched pair, rejecting a second approve with `409 already_approved` | cr-arkilaunch-f4-f5-fleet-weather.md §5 | rfc-arkilaunch-ocr-edtr-reconciliation.md §3 | [cr-arkilaunch-edtr-double-approve.md](cr-arkilaunch-edtr-double-approve.md) |
| cr-f4-f5-fleet-weather | 2026-08-02 | PRD-F4/F5 backend implementation: direct cron entrypoints (not `/internal/jobs/*`), equipment write endpoints added, `weather_alerts` doubles as the reading cache, deduction double-count finding (deferred) | build-arkilaunch.md §5.1 | sdd-arkilaunch.md §4 | [cr-arkilaunch-f4-f5-fleet-weather.md](cr-arkilaunch-f4-f5-fleet-weather.md) |

---

## 3. Incident Log (Postmortems)

| PM ID | Incident date | Severity | Summary | Action items closed? | File |
|-------|---------------|----------|---------|----------------------|------|
| (none yet) | - | - | - | - | - |

---

## 4. Health Check

- [~] Every Locked doc's **Last Reconciled** date is newer than the last code change to its area. PRD/SDD/QAD/RFC-2 headers bumped to 2026-09-07 by `cr-arkilaunch-m4-money-path-gates.md`, in the same pass as the code it describes. RFC-1 §3 was reconciled in the same pass, since migration `0016` implements it — leaving it stale would have reproduced exactly the drift the 2026-08-20 record was written to fix. Still open: RFC-3 was last reconciled 2026-08-01 and DSD 2026-08-02, neither touched since. And still no automated check catches this class of drift — the 2026-08-20 instance was found by manual review, and so was this one.
- [x] No doc has been in `Draft` longer than expected without movement.
- [~] No open Change Records. **2026-09-19:** `cr-arkilaunch-doc-reconcile-2026-09-19.md` is `Applied`. The 2026-09-19 full-system audit's four findings documents are not Change Records and do not gate on this row, but their open items are real work: of 17 HIGH findings, the API authorization gaps, the drizzle snapshot drift, the missing indexes, the money-path constraints and the OCR gate items are closed in code on `dev`; still open are the EDTR equipment-day UNIQUE (`audit-db-tenant-isolation.md` #4, blocked on a 389-row test-fixture cleanup that cascades into 343 line items and 396 reconciliation references), composite tenant-scoped FKs (#6), `payments.provider_ref` NOT NULL (#8, blocked because the refund path inserts without one), and the whole `audit-api-surface.md` #11/#12 unbuilt-client backlog. `cr-arkilaunch-m4-money-path-gates.md` is `Applied`, and is the first record to fill its §5 pre-merge gate table with real verdicts (five agents run, one FAIL found and fixed) rather than leaving it `*pending*`; it also adds the `.github/pull_request_template.md` that `cr-arkilaunch-pilot-honesty.md` §6 claimed to have added but never did. The two earlier records remain incomplete on their own terms and are **not** closed by this pass: `cr-arkilaunch-pilot-honesty.md` §5 (Verification) is still the placeholder `*(Filled in as each phase lands.)*` and its §6 gate table is still entirely `*pending*`; `cr-arkilaunch-azure-di-provisioning.md` §6 named the Deploy workflow broken on `azure/login` since 2026-08-06; `cr-arkilaunch-deploy-unblock.md` (2026-09-13) addresses the code half of that — the federated credential is now declared in the bootstrap Terraform with the `environment:` subjects that can actually match, and `deploy.yml` has a migrate step gated behind `terraform-plan`. The bootstrap was applied 2026-09-13 and both GitHub environments now carry the four variables, so every prerequisite `azure/login` failed on is in place. Not closed: no `Deploy` run has yet proven it, so the pipeline is unproven rather than fixed until the first push to `dev` goes green, and the failed first attempt stranded an ownerless app registration in a tenant only its own directory admins can clean up.
- [x] Feature IDs (`PRD-F#`) referenced by SDD / RFC / QAD exist in the PRD (PRD-F1..F8 frozen).
- [x] §1.1 Traceability Matrix matches Must-Have coverage (every Must-Have has SDD + QAD; RFCs cover F1/F3/F6/F7; PRD-F4's SDD entries filled in the 2026-08-01 pass, see below).
- [x] Metric IDs (`BRD-M#`) flow to GTM with a feeding event in PRD §5.6. (PRD §5.6 events wired to BRD-M1..M8; GTM exists, `gtm-arkilaunch.md`.)
- [x] UES `UES-E#` feed GTM pricing; `UES-D#` filled; `BRD-V#` concentrate capital. (UES drafted; `UES-D1..D8` filled; GTM exists and cites UES-E1/E3/E5, UES-D1/D5/D8.)
- [~] **Materialized artifacts match their canonical docs.** New row, because nothing was checking it. `AGENTS.md`, `BRAND.md` and `MODEL_CARD.md` diff clean. `DESIGN.md` did **not** between 2026-09-13 and 2026-09-19 -- it had been hand-edited, this section claimed otherwise, and `audit-docs-drift.md` found it; `cr-arkilaunch-doc-reconcile-2026-09-19.md` folded the content into the DSD and re-materialized. Still `[~]` rather than `[x]` because the check is manual: `materialize.py` belongs to the external FMD engine and is not vendored here, so nothing mechanically enforces the rule DSD §9 states.
- [~] **The registry lists every file on disk.** New row. Six documents were unlisted as of 2026-09-19, one of them a Change Record for shipped work -- a hole in the change log itself. Fixed in the same record. `[~]` for the same reason as above: hand-maintained, and this class of drift has now been found by manual review three times (2026-08-20, 2026-09-13, 2026-09-19). Expect it again before an automated check exists.
- [x] SAD roster matches materialized agent files. (SAD exists, `sad-arkilaunch.md`; all 5 files present in `.claude/agents/` with names, models, and tools matching the SAD cards.)
- [x] BUILD pinned versions + golden-path samples re-verified recently. (Currency pass 2026-07-25; every installable package/SDK pins an exact version as of the 2026-08-01 pass, `build-arkilaunch.md` §3; 6 managed-platform rows honestly marked n/a rather than "current".)
- [x] No open Postmortems.
- [x] **Production Readiness Gate**: at the documentation level all inputs exist (SDD §5 security + RLS, CLR register, AIA dossier with escalations tracked, QAD Must-Have + AI-abuse specified, OPS SLOs/alerts/runbooks including the deposit-deduction runbook and NPC 72h clock, PRD §9 rollback, BUILD stack pinned, DSD §8). The gate itself is defined in this section plus [pitch-arkilaunch.md](pitch-arkilaunch.md) §5 and [wrap-arkilaunch.md](wrap-arkilaunch.md) §5, not in the external FMD engine's generic `AGENTS.md` (this repo does not vendor the FMD engine). A shippable-system pass still needs the code, the QAD executed, and the CLR/AIA counsel clearances.
- [~] **Validator (external tool, not reproducible from this repo):** the FMD engine's `check.py` is not vendored here (no `fmd/` directory), so its "0 failures" result from 2026-07-25 cannot be re-run or re-verified from this repository. Treat that historical claim as unverifiable rather than current. A 2026-08-01 manual remediation pass fixed the defects a semantic reconcile would have caught: stale "pending" claims across this file, wrap, ops, dsd, and clr; broken relative links in AGENTS.md/BRAND.md; an under-specified RLS policy in RFC-3; a table-count mismatch across SDD/RFC-1/RFC-3/SAD; missing QAD test rows for the quotation engine; a UES arithmetic double-count; and more (see the LOG entry for this pass). Root `IDEA.md`'s voice violations (em/en-dashes in the source thesis) are a known, accepted exception; not a target for this pass.

---

## 5. Notes

- Scrutiny verdict: **PROCEED WITH FIXES** (2026-07-25). Carried fixes tracked in [scrutiny-arkilaunch.md](scrutiny-arkilaunch.md) §1 and mirrored as gaps G-1..G-10 §3; each lands in its named downstream doc (Open-Meteo commercial -> SDD/OPS/UES done **as of 2026-07-25; narrowed again 2026-08-20 by `cr-arkilaunch-open-meteo-free-tier.md` -- G-4's cost/quota halves are now resolved against the FREE tier, and the commercial-use restriction is carried forward as G-4's one remaining open item, not closed**; diesel source -> RFC-3 done; multi-tenancy -> RFC-1/SDD done; ISO 25010 version -> QAD done; Azure DI residency -> CLR done, AIA done with AIA-R7 tracked as the one open, escalated risk; 5-point Likert -> QAD done). G-10 (PayMongo webhook idempotency/refunds/disputes) resolved directly in SDD §4 as of 2026-08-01, no dedicated RFC needed.
- Product framing decision (build start): multi-tenant SaaS (ArkiLaunch), Almara = anchor tenant.
- Confirmed stack decisions: OCR = Azure AI Document Intelligence; payments = PayMongo; stack re-evaluated against current best practices (currency-verified 2026-07-25). Documented divergences from the thesis (persistent Azure Container Apps backend + jobs vs Vercel serverless; Drizzle vs Prisma for first-class RLS; NestJS/Passport identity vs Supabase GoTrue; Vite 8; add Playwright; Open-Meteo commercial plan **-> superseded 2026-08-20: ships against the free tier instead, see BUILD §3's divergence row**) are recorded in BUILD §3.
- Engine note: the root `IDEA.md` thesis is the long-form source of record; `docs/idea-arkilaunch.md` is its FMD-shaped distillation.
- Build note: mid-build the org hit a monthly spend limit; the later docs (RFC-3, QAD, CLR, OPS, SAD, BUILD, AIA, GTM, PITCH, WRAP, VALIDATION, VOICE) were authored inline by the orchestrator rather than via parallel subagents.
- Validation (2026-07-25): `check.py docs --scale full` = 0 failures over the generated suite + materialized artifacts. Materialization banner in AGENTS/BRAND/DESIGN was converted from an HTML comment to a blockquote so the voice check would not read the comment's closing `-->` as a spaced dash (FMD `materialize.py` quirk; a candidate engine fix noted in the WRAP field report). Root `IDEA.md` thesis still fails voice (54 em/en-dash and `--` hits); left intact as the user's source document.
- **Remediation pass (2026-08-01):** a full re-audit found the 2026-07-25 validation was a lint/voice pass, not a semantic reconcile, so several waves of later work never propagated back into earlier docs. Fixed: an under-specified RLS policy on `pricing_parameters` (RFC-3) that the `migration-rls-guardian` agent would have silently passed; a table-count mismatch (32 vs 33 vs 35) across SDD/RFC-1/RFC-3/SAD, now reconciled at 35 tables (28 tenant-owned, 7 global); PRD-F4 was a stub in the SDD (no endpoint/contract), now filled; the RFC-3 quotation engine had no QAD test rows despite BUILD/RFC-3 both citing them, now `QAD-T43`..`T48`; OPS runbooks were non-contiguous (4.3-4.7 + 4.6-iso) with two misrouted P0 alert references and no deposit-deduction-failure runbook, now renumbered 4.1-4.7 with the missing runbook added and the NPC 72-hour breach-notification clock specified; a UES arithmetic double-count (Open-Meteo booked in both variable COGS and fixed overhead) understated the illustrative breakeven by ~PHP 220/mo; BRAND.md/DESIGN.md were re-split per the DSD §9 contract (159 duplicated lines removed, missing voice link added, unsubstituted `# Brand:`/`# Design:` titles fixed); MODEL_CARD.md was regenerated against AIA §1 after 9 facts were found dropped in the original hand-authored version; AGENTS.md/BRAND.md had broken relative links from the root (14 total) now `docs/`-prefixed; and this Health Check itself was updated to stop claiming several completed docs (GTM, SAD, BUILD, root artifacts) were still pending. Full findings and fixes are logged in [log-arkilaunch.md](log-arkilaunch.md).
- **Doc-count convention:** §1's Document Suite table lists 19 doc types; together with this index (20) and the 3 RFCs, that is 23 suite-and-RFC files; with the 3 runbooks, the 30 Change Records, the 4 audit findings docs and 1 report, the filesystem holds **61** `docs/*.md` files. Earlier notes and the LOG that say "20 docs" mean the suite-plus-index count and are counting RFCs separately, not omitting them. This figure said 44 against 53 on disk until `cr-arkilaunch-doc-reconcile-2026-09-19.md`; it is hand-maintained and has now drifted twice, so treat a mismatch here as expected between reconciliation passes rather than as a signal that nothing moved.
- **Backend completion (2026-08-02):** all eight PRD features (F1..F8) now have a working, tested backend. F2/F8 (last to land, `cr-arkilaunch-f2-f8-bookings-payments.md`) shipped with two deliberate drifts from the PRD/SDD sketch: an authenticated `customer` role rather than the PRD's public/guest catalog (RFC-1's verified-JWT-only tenant rule rules out an unauthenticated write), and corrected PayMongo webhook event names against live-verified docs (`refund.succeeded` + `dispute.created`/`dispute.resolved`, not the SDD sketch's `refund.updated`/generic "dispute"). The same pass also closed the deposit double-deduction finding `cr-arkilaunch-f4-f5-fleet-weather.md` §5 had deferred (`cr-arkilaunch-edtr-double-approve.md`).
- **Backend read-surface completion (2026-08-02, `cr-arkilaunch-f9-read-surface.md`):** the eight features' backends previously exposed only their *write* half. This pass added the missing reads (billing/deposit ledger, EDTR review queue, sites/deployments/weather-advisories/incidents, notifications, financial report) plus global rate limiting, a login lockout, and a real fix to the EDTR deposit-deduction gate's own arithmetic (it previously ignored `rental_contracts.deposit_required` entirely and could never actually cap a deduction). Deferred: S18/S19 settings (rate-card writes, user/role admin), tenant registration + the platform console (S3/S25), and a dedicated dashboard-aggregate endpoint.
- **Design system marketing-tier merge + first code landing (2026-08-02, `cr-arkilaunch-dsd-marketing-tier.md`):** the DSD was fully specified but had zero implementation in `apps/web/` (a bare `@import 'tailwindcss'`, stock `slate-*`/`blue-*` on every route). This pass (a) merged a user-supplied SprintForge visual system into the DSD as a new, explicitly scoped Marketing tier (S1/S22 only) that keeps the amber/steel/IBM Plex identity and drops SprintForge's own accent color entirely, while leaving the Console tier's instrument-panel rules unchanged for every authed screen; (b) landed the Console token layer in code for the first time (`apps/web/src/index.css`, self-hosted IBM Plex + Instrument Serif fonts under `apps/web/public/fonts/`); (c) built and restyled the existing POC routes onto Button/Input/Select/Surface primitives; (d) built the four signature domain components (GaugeReadout, ConfidenceChip, HazardDivider, WeatherBanner) with colocated Vitest tests, fixing a pre-existing `apps/web` test-runner bug (Vitest's default glob was picking up the Playwright `e2e/` spec) along the way. Deferred: the Marketing-tier components (FloatingNav, FeatureTile, ProofPill, PackageCard, RiseIn) and the S1 Public Landing page itself are specified but not built.
- **Storefront landing page, auth flow, and role-aware shells (2026-08-02, `cr-arkilaunch-frontend-storefront-shell.md`):** a user-supplied Figma prototype turned out to describe Almara's own single-tenant rental storefront (closer to PRD S22 than S1) plus a large set of features absent from the frozen PRD (a negotiation flow, in-app payment credential screens, Ticket Management, Security Logs, Extend Rental, a fifth "Operator" role). Per the user's explicit decisions this pass built the intersection: a five-layout route tree (`_public`/`_auth`/`_account`/`_app`/`_field`) with role guards, the five previously-unbuilt DSD Marketing-tier components, a full storefront landing page in Yardboard tokens (Figma's ISO 9001 badge and six testimonials dropped as unverifiable/unsourced, mechanism claims kept instead), a two-step registration flow against fixtures, and a Playwright build-time prerender closing the BUILD §5.2 crawlability checklist without an SSR migration. The negotiation flow, in-app payment capture, Ticket Management, Security Logs, Extend Rental, a distinct Operator role, the registration/catalog backend, and public catalog access are logged as deferred, one future CR each.
